{
  "name": "cat-drop-2",
  "version": "1.0.0",
  "private": true,
  "description": "Cat Drop 2 — web + Android (Capacitor)",
  "scripts": {
    "start": "serve www -l tcp://0.0.0.0:${PORT:-8080} -n",
    "serve:local": "serve www -l tcp://0.0.0.0:8080 -n",
    "check": "node scripts/release-check.mjs",
    "qa": "python scripts/qa_checks.py",
    "qa:full": "node scripts/full-game-qa.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.4.0",
    "@idleflowgames/capacitor-play-games": "^0.1.1",
    "puppeteer": "^25.3.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
